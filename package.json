{
  "name": "my-first-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "build:render": "npm ci --prefix server && npm ci --prefix client && npm run prisma:generate --prefix server && npm run build --prefix server && npm run build --prefix client",
    "start:render": "npm run prisma:migrate:deploy --prefix server && npm run start --prefix server",
    "prisma:migrate": "npm run prisma:migrate --prefix server --",
    "prisma:generate": "npm run prisma:generate --prefix server",
    "seed:admin": "npm run seed:admin --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
